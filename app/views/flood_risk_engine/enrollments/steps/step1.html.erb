<%= content_for(:heading) { t(".heading") } %>

<%# Using a step partial layout to DRY up the html %>
<%= render layout: 'step', locals: { form: form, step: step } do |f| %>
  <%= f.input :dummy_boolean, as: :radio_buttons %>
<% end %>
